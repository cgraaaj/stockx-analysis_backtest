# Release Notes

Release notes are updated automatically on each push to `master` (see `.github/workflows/update-release-notes.yml`). You can also run `scripts/update-release-notes.sh` locally.

---

## 2025-03-16 (initial)

- Initial release notes file; automation for master push configured.
